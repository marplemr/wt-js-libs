<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

-   [Contracts][1]
    -   [\_\_getInstance][2]
    -   [getIndexInstance][3]
    -   [getHotelInstance][4]
    -   [decodeLogs][5]
    -   [createInstance][6]
-   [HotelDataIndex][7]
-   [LocationInterface][8]
-   [ContactInterface][9]
-   [ContactsInterface][10]
-   [AddressInterface][11]
-   [HotelDescriptionInterface][12]
-   [DataModelOptionsType][13]
-   [DataModel][14]
    -   [getWindingTreeIndex][15]
    -   [getTransactionsStatus][16]
    -   [createWallet][17]
    -   [createInstance][18]
-   [OnChainHotel][19]
    -   [initialize][20]
    -   [dataIndex][21]
    -   [setLocalData][22]
    -   [toPlainObject][23]
    -   [\_\_editInfoOnChain][24]
    -   [createOnChainData][25]
    -   [updateOnChainData][26]
    -   [removeOnChainData][27]
    -   [createInstance][28]
-   [WTIndex][29]
    -   [addHotel][30]
    -   [updateHotel][31]
    -   [removeHotel][32]
    -   [getHotel][33]
    -   [getAllHotels][34]
    -   [createInstance][35]
-   [WtLibsOptionsType][36]
-   [WTLibs][37]
    -   [getWTIndex][38]
    -   [getTransactionsStatus][39]
    -   [createInstance][40]
-   [AddHotelResponseInterface][41]
-   [HotelOnChainDataInterface][42]
-   [HotelInterface][43]
-   [WTIndexInterface][44]
-   [OffChainDataAccessorInterface][45]
-   [DataModelInterface][46]
-   [RawLogRecordInterface][47]
-   [DecodedLogRecordInterface][48]
-   [TransactionDataInterface][49]
-   [TxInterface][50]
-   [TxReceiptInterface][51]
-   [AdaptedTxResultInterface][52]
-   [AdaptedTxResultsInterface][53]
-   [WalletInterface][54]
-   [KeystoreV3Interface][55]
-   [OffChainDataClientOptionsType][56]
-   [OffChainDataClient][57]
    -   [setup][58]
    -   [\_\_reset][59]
    -   [getAccessor][60]
-   [RemotelyBackedDataset][61]
    -   [bindProperties][62]
    -   [isObsolete][63]
    -   [markObsolete][64]
    -   [isDeployed][65]
    -   [markDeployed][66]
    -   [\_genericGetter][67]
    -   [\_genericSetter][68]
    -   [updateRemoteData][69]
    -   [createInstance][70]
-   [FieldDefType][71]
-   [StoragePointer][72]
    -   [\_genericGetter][73]
    -   [\_detectSchema][74]
    -   [\_getOffChainDataClient][75]
    -   [\_downloadFromStorage][76]
    -   [createInstance][77]
-   [Utils][78]
    -   [isZeroAddress][79]
    -   [applyGasCoefficient][80]
    -   [determineDeployedContractFutureAddress][81]
    -   [getCurrentWeb3Provider][82]
    -   [getCurrentBlockNumber][83]
    -   [determineCurrentAddressNonce][84]
    -   [getTransactionReceipt][85]
    -   [getTransaction][86]
    -   [createInstance][87]
-   [Wallet][88]
    -   [setWeb3][89]
    -   [isDestroyed][90]
    -   [getAddress][91]
    -   [unlock][92]
    -   [signAndSendTransaction][93]
    -   [lock][94]
    -   [destroy][95]
    -   [createInstance][96]

## Contracts

Wrapper class for work with Winding Tree's Ethereum
smart contracts.

**Parameters**

-   `web3`  

### \_\_getInstance

Generic method for getting an instance of `web3.eth.Contract`

**Parameters**

-   `name` **[string][97]** of contract, used in errors
-   `abi` **[Object][98]** specification of contract
-   `address` **[string][97]** on which we should look for the contract


-   Throws **[Error][99]** When address is invalid
-   Throws **[Error][99]** When no code is deployed on given address

Returns **web3.eth.Contract** Resulting wrapper contract

### getIndexInstance

Returns a representation of <a href="https://github.com/windingtree/wt-contracts/blob/v0.2.0/contracts/WTIndex.sol">WTIndex.sol</a>.

**Parameters**

-   `address` **[string][97]** 

Returns **web3.eth.Contract** Instance of an Index

### getHotelInstance

Returns a representation of <a href="https://github.com/windingtree/wt-contracts/blob/v0.2.0/contracts/hotel/Hotel.sol">Hotel.sol</a>.

**Parameters**

-   `address` **[string][97]** 

Returns **web3.eth.Contract** Instance of a Hotel

### decodeLogs

Decodes ethereum transaction log values. Currently supports
events from Index and Hotel smart contracts.

**Parameters**

-   `logs` **[Array][100]&lt;[RawLogRecordInterface][101]>** in a raw format

Returns **[Array][100]&lt;[DecodedLogRecordInterface][102]>** Decoded logs

### createInstance

Returns an initialized instance

**Parameters**

-   `web3` **Web3** instance created by `new Web3(provider)`

Returns **[Contracts][103]** 

## HotelDataIndex

Index file for more data urls. This is the
initial document that blockchain is pointing to.

**Properties**

-   `descriptionUrl` **[string][97]** 

## LocationInterface

Generic GPS location.

**Properties**

-   `latitude` **[number][104]??** 
-   `longitude` **[number][104]??** 

## ContactInterface

Generic contact.

**Properties**

-   `email` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `phone` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `url` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `ethereum` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 

## ContactsInterface

A map of hotel contacts.

**Properties**

-   `general` **[ContactInterface][106]?** 

## AddressInterface

Generic address interface.

**Properties**

-   `line1` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `line2` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `zip` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `city` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `state` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `country` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 

## HotelDescriptionInterface

-   **See: [https://github.com/windingtree/wt-js-libs/issues/125][107]**

Description of additional descriptive hotel data.

**Properties**

-   `name` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `description` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `contacts` **([Promise][105]&lt;[ContactsInterface][108]?> | [ContactsInterface][108]?)?** 
-   `address` **([Promise][105]&lt;[AddressInterface][109]?> | [AddressInterface][109]?)?** 
-   `location` **([Promise][105]&lt;[LocationInterface][110]?> | [LocationInterface][110]?)?** 
-   `timezone` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `currency` **([Promise][105]&lt;[string][97]?> | [string][97]?)?** 
-   `images` **([Promise][105]&lt;[Array][100]&lt;[string][97]>?> | [Array][100]&lt;[string][97]>?)?** 
-   `amenities` **([Promise][105]&lt;[Array][100]&lt;[string][97]>?> | [Array][100]&lt;[string][97]>?)?** 
-   `createdAt` **([Promise][105]&lt;[string][97]?> | [string][97]?)** 
-   `updatedAt` **([Promise][105]&lt;[string][97]?> | [string][97]?)** 

## DataModelOptionsType

DataModelOptionsType options. May look like this:

    {
      "provider": 'http://localhost:8545',// or another Web3 provider
      "gasCoefficient": 2 // Optional, defaults to 2
    }

Type: {provider: ([string][97] \| [Object][98])?, gasCoefficient: [number][104]?}

**Properties**

-   `provider` **([string][97] \| [Object][98])?** 
-   `gasCoefficient` **[number][104]?** 

## DataModel

DataModel

**Parameters**

-   `options` **[DataModelOptionsType][111]** 

### getWindingTreeIndex

Returns an Ethereum backed Winding Tree index.

**Parameters**

-   `address` **[string][97]** 

Returns **[Promise][105]&lt;WTIndexDataProvider>** 

### getTransactionsStatus

Finds out in what state are Ethereum transactions. All logs
are decoded along the way and some metrics such as min/max blockAge
are computed. If you pass all transactions related to a single
operation (such as updateHotel), you may benefit from the computed
metrics.

**Parameters**

-   `txHashes` **[Array][100]&lt;[string][97]>** 

Returns **[Promise][105]&lt;[AdaptedTxResultsInterface][112]>** 

### createWallet

Returns a wallet instance for given JSON keystore.

**Parameters**

-   `jsonWallet` **[KeystoreV3Interface][113]** 

Returns **[Promise][105]&lt;[Wallet][114]>** 

### createInstance

Creates a configured DataModel instance.

**Parameters**

-   `options` **[DataModelOptionsType][111]** 

Returns **[DataModel][115]** 

## OnChainHotel

Wrapper class for a hotel backed by a smart contract on
Ethereum that's holding `url` pointer to its data.

It provides an accessor to such data in a form of
`StoragePointer` instance under `dataIndex` property.
Every schema-specific implementation details
are dealt with in StoragePointer.

**Parameters**

-   `web3Utils` **[Utils][116]** 
-   `web3Contracts` **[Contracts][103]** 
-   `indexContract` **[Object][98]** 
-   `address` **[string][97]** 

### initialize

Initializes the underlying RemotelyBackedDataset that actually
communicates with the on-chain stored data. If address was provided
in the contsructor, the RemotelyBackedDataset is marked as deployed
and can be used instantly.

Returns **[Promise][105]&lt;void>** 

### dataIndex

Async getter for `StoragePointer` instance.
Since it has to eventually access the `url`
field stored on-chain, it is lazy loaded.
Any data structure that is accessed by StoragePointer
instance has for now be known beforehand, thus the whole
data format of hotel data on `url` is specified here.

Returns **[Promise][105]&lt;[StoragePointer][117]>** 

### setLocalData

Update manager and url properties. Url can never be nulled. Manager
can never be nulled. Manager can be changed only for an un-deployed
contract (without address).

**Parameters**

-   `newData` **[HotelOnChainDataInterface][118]** 

Returns **[Promise][105]&lt;void>** 

### toPlainObject

Helper method that returns a plain object only
with properties with data.

It is not recursive for now, i. e. all off-chain data
has to be accessed separately. Subject to change.

Returns **[Promise][105]&lt;[HotelOnChainDataInterface][118]>** 

### \_\_editInfoOnChain

Updates url on-chain. Used internally as a remoteSetter for `url` property.

**Parameters**

-   `wallet` **[WalletInterface][119]** that signs the transaction
-   `transactionOptions` **[Object][98]** usually contains from and to. Gas is automatically computed.

Returns **[Promise][105]&lt;[string][97]>** resulting transaction hash

### createOnChainData

Creates new hotel contract on-chain.

Precomputes the deployed hotel on-chain address, so even if
the resulting transaction is not yet mined, the address is already known.

Returns once the transaction is signed and sent to network by `wallet`.

**Parameters**

-   `wallet` **[WalletInterface][119]** that signs the transaction
-   `transactionOptions` **[Object][98]** usually contains from and to. Gas is automatically computed.

Returns **[Promise][105]&lt;[Array][100]&lt;[string][97]>>** list of resulting transaction hashes

### updateOnChainData

Updates all hotel-related data by calling `updateRemoteData` on a `RemotelyBackedDataset`
dataset.

**Parameters**

-   `wallet` **[WalletInterface][119]** that signs the transaction
-   `transactionOptions` **[Object][98]** usually contains from and to. Gas is automatically computed.


-   Throws **[Error][99]** When the underlying contract is not yet deployed.
-   Throws **[Error][99]** When url is empty.

Returns **[Promise][105]&lt;[Array][100]&lt;[string][97]>>** List of transaction hashes

### removeOnChainData

Destroys the object on network, in this case, calls a `deleteHotel` on
Winding Tree index contract.

**Parameters**

-   `wallet` **[WalletInterface][119]** that signs the transaction
-   `transactionOptions` **[Object][98]** usually contains from and to. Gas is automatically computed.


-   Throws **[Error][99]** When the underlying contract is not yet deployed.

Returns **[Promise][105]&lt;[Array][100]&lt;[string][97]>>** List of transaction hashes

### createInstance

Create new configured instance.

**Parameters**

-   `web3Utils` **[Utils][116]** 
-   `web3Contracts` **[Contracts][103]** 
-   `indexContract` **web3.eth.Contract** Representation of Winding Tree index
-   `address` **[string][97]** is an optional pointer to Ethereum network where the hotel lives.
    It is used as a reference for on-chain stored data. If it is not provided, a hotel has
    to be created on chain to behave as expected.

Returns **[OnChainHotel][120]** 

## WTIndex

Ethereum smart contract backed implementation of Winding Tree
index wrapper. It provides methods for working with hotel
contracts.

**Parameters**

-   `indexAddress` **[string][97]** 
-   `web3Utils` **[Utils][116]** 
-   `web3Contracts` **[Contracts][103]** 

### addHotel

Adds a totally new hotel on chain. Does not wait for the transactions
to be mined, but as fast as possible returns a list of transaction IDs
and the new hotel on chain address.

**Parameters**

-   `wallet` **[WalletInterface][119]** 
-   `hotelData` **[HotelOnChainDataInterface][118]** 


-   Throws **[Error][99]** When hotelData does not contain url property.
-   Throws **[Error][99]** When anything goes wrong during communication with the network.

Returns **[Promise][105]&lt;[AddHotelResponseInterface][121]>** 

### updateHotel

Updates a hotel on chain. Does not wait for the transactions
to be mined, but as fast as possible returns a list of transaction
IDs so you can keep track of the progress.

**Parameters**

-   `wallet` **[WalletInterface][119]** 
-   `hotel` **[HotelInterface][122]** 


-   Throws **[Error][99]** When hotel does not have a manager field.
-   Throws **[Error][99]** When anything goes wrong during communication with the network.

Returns **[Promise][105]&lt;[Array][100]&lt;[string][97]>>** 

### removeHotel

Removes the hotel from chain. Does not wait for the transactions
to be mined, but as fast as possible returns a list of transaction
IDs so you can keep track of the progress.

**Parameters**

-   `wallet` **[WalletInterface][119]** 
-   `hotel` **[HotelInterface][122]** 


-   Throws **[Error][99]** When anything goes wrong such as-   hotel does not exist
    -   hotel does not belong to the calling manager
    -   not enough gas

Returns **[Promise][105]&lt;[Array][100]&lt;[string][97]>>** 

### getHotel

Gets hotel representation of a hotel on a given address. If hotel
on such address is not registered through this Winding Tree index
instance, the method throws immediately.

**Parameters**

-   `address` **[string][97]** 


-   Throws **[Error][99]** When hotel does not exist.
-   Throws **[Error][99]** When something breaks in the network communication.

Returns **[Promise][105]&lt;[HotelInterface][122]?>** 

### getAllHotels

Returns a list of all hotels. It will filter out
every hotel that is inaccessible for any reason.

Currently does not provide any information on inaccessible
hotels. Subject to change.

Returns **[Promise][105]&lt;[Array][100]&lt;[HotelInterface][122]>>** 

### createInstance

Returns a configured instance of WTIndex
representing a Winding Tree index contract on a given `indexAddress`.

**Parameters**

-   `indexAddress` **[string][97]** 
-   `web3Utils` **[Utils][116]** 
-   `web3Contracts` **[Contracts][103]** 

Returns **[Promise][105]&lt;[WTIndex][123]>** 

## WtLibsOptionsType

General options for wt-libs-js. Holds all things necessary
for successful setup of Winding Tree network.

Type: {dataModelOptions: [DataModelOptionsType][111], offChainDataOptions: [OffChainDataClientOptionsType][124]}

**Properties**

-   `dataModelOptions` **[DataModelOptionsType][111]** 
-   `offChainDataOptions` **[OffChainDataClientOptionsType][124]** 

## WTLibs

Main public interface of wt-libs-js.

**Parameters**

-   `options` **[WtLibsOptionsType][125]** 

### getWTIndex

Get an instance of Winding Tree index from the underlying `data-model`.

**Parameters**

-   `address` **[string][97]** of the Winding Tree index

Returns **[Promise][105]&lt;[WTIndexInterface][126]>** 

### getTransactionsStatus

Get a transactions status from the underlying `data-model`

**Parameters**

-   `transactionHashes` **[Array][100]&lt;[string][97]>** 

Returns **[Promise][105]&lt;[AdaptedTxResultsInterface][112]>** 

### createInstance

Call this to create wt-libs-js instance.

**Parameters**

-   `options` **[WtLibsOptionsType][125]** 

Returns **[WTLibs][127]** WTLibs

## AddHotelResponseInterface

Response of the addHotel operation.

-   `address` holds the projected address of newly created hotel.
-   `transactionIds` contains an array of ids of related transactions
    that had to be sent to the underlying networks. You can
    use these ids to check on the asynchronous operation state.

**Properties**

-   `address` **[string][97]?** 
-   `transactionIds` **[Array][100]&lt;[string][97]>** 

## HotelOnChainDataInterface

Shape of data that is stored on-chain
about every hotel.

-   `address` is the network address.
-   `manager` is the network address of hotel manager.
-   `url` holds a pointer to the off-chain storage
    that is used internally to store data.

**Properties**

-   `address` **([Promise][105]&lt;[string][97]?> | [string][97]?)** 
-   `manager` **([Promise][105]&lt;[string][97]?> | [string][97]?)** 
-   `url` **([Promise][105]&lt;[string][97]?> | [string][97]?)** 

## HotelInterface

**Extends HotelOnChainDataInterface**

Represents a hotel instance that can
communicate with on-chain hotel representation
and provides an access to offChain data via `dataIndex`
property.

**Properties**

-   `dataIndex` **[Promise][105]&lt;[StoragePointer][117]>** 
-   `toPlainObject` **function (): [Promise][105]&lt;[Object][98]>** 
-   `setLocalData` **function (newData: [HotelOnChainDataInterface][118]): [Promise][105]&lt;void>** 
-   `createOnChainData` **function (wallet: [WalletInterface][119], transactionOptions: [Object][98]): [Promise][105]&lt;[Array][100]&lt;[string][97]>>** 
-   `updateOnChainData` **function (wallet: [WalletInterface][119], transactionOptions: [Object][98]): [Promise][105]&lt;[Array][100]&lt;[string][97]>>** 
-   `removeOnChainData` **function (wallet: [WalletInterface][119], transactionOptions: [Object][98]): [Promise][105]&lt;[Array][100]&lt;[string][97]>>** 

## WTIndexInterface

WindingTree index interface that provides all methods
necessary for interaction with the hotels.\`

**Properties**

-   `addHotel` **function (wallet: [WalletInterface][119], hotel: [HotelOnChainDataInterface][118]): [Promise][105]&lt;[AddHotelResponseInterface][121]>** 
-   `getHotel` **function (address: [string][97]): [Promise][105]&lt;[HotelInterface][122]?>** 
-   `getAllHotels` **function (): [Promise][105]&lt;[Array][100]&lt;[HotelInterface][122]>>** 
-   `updateHotel` **function (wallet: [WalletInterface][119], hotel: [HotelInterface][122]): [Promise][105]&lt;[Array][100]&lt;[string][97]>>** 
-   `removeHotel` **function (wallet: [WalletInterface][119], hotel: [HotelInterface][122]): [Promise][105]&lt;[Array][100]&lt;[string][97]>>** 

## OffChainDataAccessorInterface

Interface for an off-chain storage read.

**Properties**

-   `upload` **function (data: {}): [Promise][105]&lt;[string][97]>** 
-   `update` **function (url: [string][97], data: {}): [Promise][105]&lt;[string][97]>** 
-   `download` **function (url: [string][97]): [Promise][105]&lt;{}?>** 

## DataModelInterface

Formalization of DataModel's public interface.

**Properties**

-   `getWindingTreeIndex` **function (address: [string][97]): [Promise][105]&lt;[WTIndexInterface][126]>** 
-   `getTransactionsStatus` **function (transactionHashes: [Array][100]&lt;[string][97]>): [Promise][105]&lt;[AdaptedTxResultsInterface][112]>** 
-   `createWallet` **function (jsonWallet: [Object][98]): [Promise][105]&lt;[WalletInterface][119]>** 

## RawLogRecordInterface

This interface represents raw ethereum transaction log object
as returned by <a href="http://web3js.readthedocs.io/en/1.0/web3-eth.html#eth-getpastlogs-return">getPastLogs</a>.
Sometimes you might need the raw data to do some additional processing.

**Properties**

-   `address` **[string][97]** 
-   `data` **[string][97]** 
-   `topics` **[Array][100]&lt;[string][97]>** 
-   `logIndex` **[number][104]** 
-   `transactionIndex` **[number][104]** 
-   `transactionHash` **[number][104]** 
-   `blockHash` **[number][104]** 
-   `blockNumber` **[number][104]** 

## DecodedLogRecordInterface

This interface represents an Ethereum log record
with decoded data that are much easier to read
and act upon.

**Properties**

-   `address` **[string][97]** 
-   `event` **[string][97]** 
-   `attributes` **[Array][100]&lt;{name: [string][97], type: [string][97], value: [string][97]}>** 

## TransactionDataInterface

Ethereum transaction data used when creating transaction, see for example
[https://web3js.readthedocs.io/en/1.0/web3-eth-accounts.html#signtransaction][128]

**Properties**

-   `nonce` **([string][97] \| [number][104])?** 
-   `chainId` **[string][97]?** 
-   `from` **[string][97]?** 
-   `to` **[string][97]** 
-   `data` **[string][97]** 
-   `value` **[string][97]?** 
-   `gasPrice` **[string][97]?** 
-   `gas` **([string][97] \| [number][104])** 

## TxInterface

Ethereum transaction data after TX was accepted by the network, see
for example [http://web3js.readthedocs.io/en/1.0/web3-eth.html#gettransaction][129]

**Properties**

-   `hash` **[string][97]?** 
-   `nonce` **([string][97] \| [number][104])?** 
-   `blockHash` **[string][97]?** 
-   `blockNumber` **[number][104]?** 
-   `transactionIndex` **[number][104]?** 
-   `from` **[string][97]?** 
-   `to` **[string][97]?** 
-   `value` **[string][97]?** 
-   `gasPrice` **[string][97]?** 
-   `gas` **[number][104]?** 
-   `input` **[string][97]?** 

## TxReceiptInterface

Transaction receipt as returned by
<a href="http://web3js.readthedocs.io/en/1.0/web3-eth.html#eth-gettransactionreceipt-return">getTransactionReceipt</a>.
This raw data might be sometimes needed for additional processing.

**Properties**

-   `transactionHash` **[string][97]** 
-   `blockNumber` **[number][104]** 
-   `blockHash` **[string][97]** 
-   `transactionIndex` **[number][104]** 
-   `from` **[string][97]** 
-   `to` **[string][97]** 
-   `contractAddress` **any** 
-   `cumulativeGasUsed` **[number][104]** 
-   `gasUsed` **[number][104]** 
-   `logs` **[Array][100]&lt;[RawLogRecordInterface][101]>** 
-   `status` **[number][104]** 

## AdaptedTxResultInterface

A custom transaction result interface that informs
about the transaction status, its age and decoded logs.

**Properties**

-   `transactionHash` **[string][97]** 
-   `blockAge` **[number][104]** 
-   `decodedLogs` **[Array][100]&lt;[DecodedLogRecordInterface][102]>** 
-   `raw` **[TxReceiptInterface][130]** 

## AdaptedTxResultsInterface

A cummulative result of multiple transactions. We are
computing how many of the transactions were already
executed, how old are they (which might be useful for making
assumptions about confirmations). This also contains the raw data.

**Properties**

-   `meta` **{total: [number][104], processed: [number][104], minBlockAge: [number][104], maxBlockAge: [number][104], allPassed: [boolean][131]}** 
-   `meta.total` **[number][104]** 
-   `meta.processed` **[number][104]** 
-   `meta.minBlockAge` **[number][104]** 
-   `meta.maxBlockAge` **[number][104]** 
-   `meta.allPassed` **[boolean][131]** 
-   `results` **{}?** 

## WalletInterface

Wallet abstraction interface. It assumes the following workflow:
1\. libs user holds a json wallet
2\. libs user unlocks the wallet abstraction with a password
3\. libs user calls some business logic which internally uses `signAndSendTransaction`
4\. libs user either locks the wallet (if she plans to use it again)
4\. OR destroys the wallet object data by calling `destroy`

`lock` should not remove the data necessary for unlocking the wallet again.
`destroy` on the other hand should clean all data that may be exploited from memory

**Properties**

-   `unlock` **function (password: [string][97]): void** 
-   `signAndSendTransaction` **function (transactionData: [TransactionDataInterface][132], onReceipt: function (receipt: [TxReceiptInterface][130]): void?): [Promise][105]&lt;[string][97]>** 
-   `lock` **function (): void** 
-   `destroy` **function (): void** 
-   `getAddress` **function (): [string][97]** 

## KeystoreV3Interface

Interface for Ethereum keystore

Description: [https://medium.com/@julien.m./what-is-an-ethereum-keystore-file-86c8c5917b97][133]

Specification: [https://github.com/ethereum/wiki/wiki/Web3-Secret-Storage-Definition][134]

**Properties**

-   `version` **[number][104]** 
-   `id` **[string][97]** 
-   `address` **[string][97]** 
-   `crypto` **{ciphertext: [string][97], cipherparams: {iv: [string][97]}, cipher: [string][97], kdf: [string][97], kdfparams: {dklen: [number][104], salt: [string][97], n: [number][104], r: [number][104], p: [number][104]}, mac: [string][97]}** 
-   `crypto.ciphertext` **[string][97]** 
-   `crypto.cipherparams` **{iv: [string][97]}** 
-   `crypto.cipherparams.iv` **[string][97]** 
-   `crypto.cipher` **[string][97]** 
-   `crypto.kdf` **[string][97]** 
-   `crypto.kdfparams` **{dklen: [number][104], salt: [string][97], n: [number][104], r: [number][104], p: [number][104]}** 
-   `crypto.kdfparams.dklen` **[number][104]** 
-   `crypto.kdfparams.salt` **[string][97]** 
-   `crypto.kdfparams.n` **[number][104]** 
-   `crypto.kdfparams.r` **[number][104]** 
-   `crypto.kdfparams.p` **[number][104]** 
-   `crypto.mac` **[string][97]** 

## OffChainDataClientOptionsType

OffChainDataClientOptionsType

Type: {accessors: {}}

**Properties**

-   `accessors` **{}** 

## OffChainDataClient

OffChainDataClient is a static factory class that is responsible
for creating proper instances of OffChainDataAccessorInterface.
It is configured during the library initialization.

Please bear in mind, that once the accessors are configured, the
configuration is shared during the whole runtime.

### setup

Initializes the map of OffChainDataAccessors.

**Parameters**

-   `options` **[OffChainDataClientOptionsType][124]** 
-   `OffChainDataClientOptionsType`  

### \_\_reset

Drops all pre-configured OffChainDataAccessors. Useful for testing.

### getAccessor

Returns a fresh instance of an appropriate OffChainDataAccessor by
calling the `create` function from the accessor's configuration.

**Parameters**

-   `schema` **[string][97]?** 


-   Throws **[Error][99]** when schema is not defined or accessor for this schema does not exist

Returns **[Promise][105]&lt;[OffChainDataAccessorInterface][135]>** 

## RemotelyBackedDataset

Dataset ready to use various strategies for storing the data
in a remote storage. Every field backed by this strategy should
have a getter and setter defined that interacts with the
remote storage. The dataset strategy may be in the following states:

-   fresh - Purely in-memory created object with no data
-   unsynced - Some data may be set locally, but they were not
    propagated to the remote storage, and no data was loaded from the
    remote storage.
-   deployed - Data has its representation ready on the remote storage
    and we may call remote getters and setters to interact with the remote storage.
-   obsolete - Data lost its representation on the remote storage and
    we should not interact with it anymore.

Internally a state is held for each property. If you `get` a property
that was not previously accessed, the whole dataset gets synced (this might
get more efficient in the future), and you get a current value from
the remote storage. If any value was changed locally, it is considered
as the current value. Once you are done with data modification, you
have to call `updateRemoteData` that propagates the whole dataset to the
remote storage. These calls are deduplicated, so if a single call is used
to update multiple properties, only once call is done.

### bindProperties

Creates generic getters and setters that proxy `remoteGetter` and
`remoteSetter` when necessary.

The fields are specified as an `options.fields` property and every key
represents a single property. Every property's options can than hold
a `remoteGetter` and `remoteSetter` field such as

    {fields: {
        url: {
          remoteGetter: async (): Promise<?string> => {
            return (await this.contract.url().call();
          },
          // this will usually return a transaction ID
          remoteSetter: async (): Promise<string> => {
            return this.contract.methods.callHotel(this.address, data).send(txOptions);
          }
    },

All passed fields are set as unsynced which means that
after the first `get` on any of those, the whole dataset will
be synced from the remote storage (if the dataset is marked as deployed).

**Parameters**

-   `options` **[Object][98]** `{fields: {[field]: fieldOptions}}`
-   `bindTo` **[Object][98]** Object to which the properties will be bound.
    Typically the initiator of this operation.

### isObsolete

Is dataset marked as obsolete?

Returns **[Boolean][131]** 

### markObsolete

Marks dataset as obsolete. Typically called after the remote storage
is destroyed or made inaccessible. This is not propagated anywhere
but merely serves as a flag to prevent further interaction with this object.

### isDeployed

Is dataset deployed to the remote storage?

Returns **[Boolean][131]** 

### markDeployed

Marks dataset as deployed. Typically called when the remote
storage is set up, created or connected to.

### \_genericGetter

Tries to get a value. If the property was not synced before,
it will sync the whole dataset from a remote storage. If a property
was modified locally before, the modified value will be returned.

**Parameters**

-   `property` **[string][97]** 


-   Throws **[Error][99]** When dataset is marked as obsolete

Returns **any** property's current value

### \_genericSetter

Sets a new value locally and marks the property as dirty. Thath
means that even after syncing data from remote storage, the object will still
serve the locally modified value.

**Parameters**

-   `property` **[string][97]** 
-   `newValue` **any** 

### updateRemoteData

Calls all remoteSetters if relevant data was changed.
Calls are deduplicated, so if the same method would be used
to update multiple fields, it is called only once.

**Parameters**

-   `wallet` **[WalletInterface][119]** that signs the transaction
-   `transactionOptions` **[Object][98]** passed to every remoteSetter, typically something like `{from: address, to: address}`

Returns **[Array][100]&lt;any>** Results of remoteSetters, it would typically contain transaction IDs

### createInstance

Generic factory method.

## FieldDefType

Definition of a data field that is stored off-chain.
This may be recursive.

Type: {name: [string][97], isStoragePointer: [boolean][131]?, fields: [Array][100]&lt;([FieldDefType][136] \| [string][97])>?}

**Properties**

-   `name` **[string][97]** 
-   `isStoragePointer` **[boolean][131]?** 
-   `fields` **[Array][100]&lt;([FieldDefType][136] \| [string][97])>?** 

## StoragePointer

`StoragePointer` serves as a representation of an
off-chain document holding JSON data. This generic class
does not enforce any protocol/schema and contains
infrastructure code that helps to set up field definition
and getters for every data field. It does not provide any
means of writing data, its sole purpose is for reading.

It is possible to use this recursively, so you can define
a field as another storage pointer. The configuration is
declarative and may look like this:

    const pointer = StoragePointer.createInstance('some://url', ['name', 'description']);
    pointer.ref; // contains 'some://url',
    await pointer.contents.name;
    await pointer.contents.description;

Or in recursive cases:

    const pointer = StoragePointer.createInstance('some://url', [
      {
        name: 'description',
        isStoragePointer: true,
        fields: ['name', 'description', 'location'],
      },
      'signature'
    ]);
    pointer.ref; // contains  'some://url'
    await pointer.contents.signature;
    const descPointer = await pointer.contents.description;
    descPointer.ref; // contains whatever is written in a description property in a document located on 'some://url'
    await descPointer.contents.name;

Only a top-level properties have to be defined beforehand, so the `signature`
field above may contain a complex JSON object.

**Parameters**

-   `url` **[string][97]** where to look for the data
-   `fields` **[Array][100]&lt;[FieldDefType][136]>** definition from which are generated getters

### \_genericGetter

Lazy data getter. The contents file gets downloaded only
once any data field is accessed for the first time. Also
the recursive `StoragePointer`s are created here only
after the contents of the data is known, because we need
to know the url to be able to instantiate the appropariate
`StoragePointer`.

This behaviour might change in a way that we are able to
swap the StoragePointer implementation during runtime.

**Parameters**

-   `field` **[string][97]** 

Returns **([StoragePointer][117] \| [Object][98])** 

### \_detectSchema

Detects schema from an url, i. e.
from `json://some-data`, detects `json`.

**Parameters**

-   `url` **[string][97]** 

Returns **[string][97]?** 

### \_getOffChainDataClient

Returns appropriate implementation of `OffChainDataAccessorInterface`
based on schema. Uses `OffChainDataClient.getAccessor` factory method.

Returns **[Promise][105]&lt;[OffChainDataAccessorInterface][135]>** 

### \_downloadFromStorage

Gets the data document via `OffChainDataAccessorInterface`.
If nothing is returned, might return an empty object.

Returns **[Promise][105]&lt;{}>** 

### createInstance

Returns a new instance of StoragePointer.

Normalizes the `fields` format before creating the actual
instance

**Parameters**

-   `url` **[string][97]** where to look for data document. It has to include schema, i. e. `https://example.com/data`
-   `fields` **[Array][100]&lt;([FieldDefType][136] \| [string][97])>** list of top-level fields in the referred document

Returns **[StoragePointer][117]** 

## Utils

Collection of utility methods useful during
communication with Ethereum network.

**Parameters**

-   `gasCoefficient` **[number][104]** 
-   `web3` **Web3** 

### isZeroAddress

Is address a zero address? Uses a bignumber.js test

**Parameters**

-   `address` **[string][97]** 

Returns **[boolean][131]** 

### applyGasCoefficient

Multiplies the gas with a previously configured `gasCoefficient`

**Parameters**

-   `gas` **[number][104]** 

Returns **[number][104]** 

### determineDeployedContractFutureAddress

Determines the future address of a deployed contact if such
contact is deployed in a transaction originating from `sender`
with `nonce`. Uses `ethereumjs-util` implementation
and always returns a checksum formatted Ethereum address.

Type: [string][97]

**Parameters**

-   `sender` **[string][97]** 
-   `nonce` **[number][104]** 

Returns **[string][97]** 

### getCurrentWeb3Provider

Proxy method for `web3.currentProvider`

Returns **[Object][98]** 

### getCurrentBlockNumber

Proxy method for `web3.eth.getBlockNumber`

Returns **[Promise][105]&lt;[number][104]>** 

### determineCurrentAddressNonce

Returns current number of transactions mined for given
Ethereum address

**Parameters**

-   `address` **[string][97]** 

Returns **[Promise][105]&lt;[number][104]>** number

### getTransactionReceipt

Proxy method for `web3.eth.getTransactionReceipt`

**Parameters**

-   `txHash` **[string][97]** 

Returns **[TxReceiptInterface][130]** 

### getTransaction

Proxy method for `web3.eth.getTransaction`

**Parameters**

-   `txHash` **[string][97]** 

Returns **[Promise][105]&lt;[TxInterface][137]>** 

### createInstance

Returns an initialized instance

**Parameters**

-   `gasCoefficient` **[number][104]** 
-   `web3` **Web3** instance created by `new Web3(provider)`

Returns **[Contracts][103]** 

## Wallet

Web3 based wallet implementation

**Parameters**

-   `keystoreJsonV3` **[KeystoreV3Interface][113]** 

### setWeb3

Sets up an initialized Web3 instance for later use

**Parameters**

-   `web3` **Web3** 

### isDestroyed

It is not possible to do any operations on a destroyed
wallet. Wallet is destroyed by calling the `destroy()` method.

Returns **[boolean][131]** 

### getAddress

Returns the address passed in `keystoreJsonV3`
in a checksummed format, e.g. prefixed with 0x
and case-sensitive.

-   Throws **[Error][99]** When wallet was destroyed.
-   Throws **[Error][99]** When there's no keystore

Returns **[string][97]** 

### unlock

Unlocks/decrypts the JSON wallet keystore. <strong>From now on
there is a readable privateKey stored in memory!</strong>

**Parameters**

-   `password` **[string][97]** 


-   Throws **[Error][99]** When wallet was destroyed.
-   Throws **[Error][99]** When there is no web3 instance configured.

### signAndSendTransaction

Takes transaction data, signs them with an unlocked private key and sends them to
the network. Resolves immediately after receiving a `transactionHash` event and optionally
may run an `onReceipt` callback after receiving the `receipt` event.

**Parameters**

-   `transactionData` **[TransactionDataInterface][132]** 
-   `onReceipt` **function (receipt: [TxReceiptInterface][130]): void?** 


-   Throws **[Error][99]** When wallet was destroyed.
-   Throws **[Error][99]** When there is no web3 instance configured.
-   Throws **[Error][99]** When wallet is not unlocked.
-   Throws **[Error][99]** When transaction.from does not match the wallet account.

Returns **[Promise][105]&lt;[string][97]>** transaction hash

### lock

Locks the wallet, i. e. deletes the private key from memory.
The original JSON keystore remains in the memory and can
be unlocked again if necessary.

This relies on the JS garbage collector, so please do not reference
the internal variables of this class elsewhere.

-   Throws **[Error][99]** When wallet was destroyed.

### destroy

Destroys the wallet. It first locks it, thus deleting
the private key from memory and then removes from
memory the JSON file.

This relies on the JS garbage collector, so please do not reference
the internal variables of this class elsewhere.

-   Throws **[Error][99]** When wallet was destroyed.

### createInstance

Creates an initialized instance

**Parameters**

-   `keystoreJsonV3` **[KeystoreV3Interface][113]** 

Returns **[Wallet][114]** 

[1]: #contracts

[2]: #__getinstance

[3]: #getindexinstance

[4]: #gethotelinstance

[5]: #decodelogs

[6]: #createinstance

[7]: #hoteldataindex

[8]: #locationinterface

[9]: #contactinterface

[10]: #contactsinterface

[11]: #addressinterface

[12]: #hoteldescriptioninterface

[13]: #datamodeloptionstype

[14]: #datamodel

[15]: #getwindingtreeindex

[16]: #gettransactionsstatus

[17]: #createwallet

[18]: #createinstance-1

[19]: #onchainhotel

[20]: #initialize

[21]: #dataindex

[22]: #setlocaldata

[23]: #toplainobject

[24]: #__editinfoonchain

[25]: #createonchaindata

[26]: #updateonchaindata

[27]: #removeonchaindata

[28]: #createinstance-2

[29]: #wtindex

[30]: #addhotel

[31]: #updatehotel

[32]: #removehotel

[33]: #gethotel

[34]: #getallhotels

[35]: #createinstance-3

[36]: #wtlibsoptionstype

[37]: #wtlibs

[38]: #getwtindex

[39]: #gettransactionsstatus-1

[40]: #createinstance-4

[41]: #addhotelresponseinterface

[42]: #hotelonchaindatainterface

[43]: #hotelinterface

[44]: #wtindexinterface

[45]: #offchaindataaccessorinterface

[46]: #datamodelinterface

[47]: #rawlogrecordinterface

[48]: #decodedlogrecordinterface

[49]: #transactiondatainterface

[50]: #txinterface

[51]: #txreceiptinterface

[52]: #adaptedtxresultinterface

[53]: #adaptedtxresultsinterface

[54]: #walletinterface

[55]: #keystorev3interface

[56]: #offchaindataclientoptionstype

[57]: #offchaindataclient

[58]: #setup

[59]: #__reset

[60]: #getaccessor

[61]: #remotelybackeddataset

[62]: #bindproperties

[63]: #isobsolete

[64]: #markobsolete

[65]: #isdeployed

[66]: #markdeployed

[67]: #_genericgetter

[68]: #_genericsetter

[69]: #updateremotedata

[70]: #createinstance-5

[71]: #fielddeftype

[72]: #storagepointer

[73]: #_genericgetter-1

[74]: #_detectschema

[75]: #_getoffchaindataclient

[76]: #_downloadfromstorage

[77]: #createinstance-6

[78]: #utils

[79]: #iszeroaddress

[80]: #applygascoefficient

[81]: #determinedeployedcontractfutureaddress

[82]: #getcurrentweb3provider

[83]: #getcurrentblocknumber

[84]: #determinecurrentaddressnonce

[85]: #gettransactionreceipt

[86]: #gettransaction

[87]: #createinstance-7

[88]: #wallet

[89]: #setweb3

[90]: #isdestroyed

[91]: #getaddress

[92]: #unlock

[93]: #signandsendtransaction

[94]: #lock

[95]: #destroy

[96]: #createinstance-8

[97]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[98]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[99]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Error

[100]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[101]: #rawlogrecordinterface

[102]: #decodedlogrecordinterface

[103]: #contracts

[104]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[105]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise

[106]: #contactinterface

[107]: https://github.com/windingtree/wt-js-libs/issues/125

[108]: #contactsinterface

[109]: #addressinterface

[110]: #locationinterface

[111]: #datamodeloptionstype

[112]: #adaptedtxresultsinterface

[113]: #keystorev3interface

[114]: #wallet

[115]: #datamodel

[116]: #utils

[117]: #storagepointer

[118]: #hotelonchaindatainterface

[119]: #walletinterface

[120]: #onchainhotel

[121]: #addhotelresponseinterface

[122]: #hotelinterface

[123]: #wtindex

[124]: #offchaindataclientoptionstype

[125]: #wtlibsoptionstype

[126]: #wtindexinterface

[127]: #wtlibs

[128]: https://web3js.readthedocs.io/en/1.0/web3-eth-accounts.html#signtransaction

[129]: http://web3js.readthedocs.io/en/1.0/web3-eth.html#gettransaction

[130]: #txreceiptinterface

[131]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Boolean

[132]: #transactiondatainterface

[133]: https://medium.com/@julien.m./what-is-an-ethereum-keystore-file-86c8c5917b97

[134]: https://github.com/ethereum/wiki/wiki/Web3-Secret-Storage-Definition

[135]: #offchaindataaccessorinterface

[136]: #fielddeftype

[137]: #txinterface
